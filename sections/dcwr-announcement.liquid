<div class="dcwr-announcement dcwr-label">{{ section.settings.text }}</div>

{% schema %}
{
  "name": "DCWR announcement",
  "settings": [
    { "type": "text", "id": "text", "label": "Text", "default": "Free shipping on orders over 50 EUR" }
  ]
}
{% endschema %}
