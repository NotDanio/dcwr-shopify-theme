<div class="dcwr-container" style="padding:4rem 1.25rem;">
  <h1>{{ blog.title }}</h1>
  {% for article in blog.articles %}<p><a href="{{ article.url }}">{{ article.title }}</a></p>{% endfor %}
</div>
