<div class="dcwr-container" style="padding:4rem 1.25rem;">
  <h1>Collections</h1>
  {% for collection in collections %}<p><a href="{{ collection.url }}">{{ collection.title }}</a></p>{% endfor %}
</div>
