<section class="dcwr-grid-section">
  <div class="dcwr-container">
    <h1 class="dcwr-grid-section__title">Collections</h1>
    {% for collection in collections %}
      <p><a href="{{ collection.url }}">{{ collection.title }}</a></p>
    {% endfor %}
  </div>
</section>
