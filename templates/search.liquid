<section class="dcwr-grid-section">
  <div class="dcwr-container">
    <h1 class="dcwr-grid-section__title">Search</h1>
    {% for item in search.results %}
      <p><a href="{{ item.url }}">{{ item.title }}</a></p>
    {% endfor %}
  </div>
</section>
